# 旧成员关系丢失排查

旧版joined_at固定为进程启动时间。旧迁移a71c5e4d2b90按“加入早于建房”删除成员，正常房主及成员都可能被删；f16d80d97501随后只补房主。

使用Git中原迁移，在内存数据库实测：两房间4条成员→清理后0条→房主修复后2条房主；正常时间且外键关闭时4条均保留。另证实外键开启时重建rooms也会级联清空成员；当前Alembic默认关闭外键，前一种原因更符合项目代码。生产触发路径无法仅凭最终数据确定。

已添加旧时间戳清理前保护。未修改生产数据、未恢复成员；先前仅归因列表加载不充分。
