# 房主进入房间权限修复

原因：本地房间1的owner_id为1，但成员表中该房间无记录；列表按房主字段显示归属，HTTP及WebSocket鉴权只查成员表，导致403。成员记录丢失的历史原因尚未确定。

修复：统一角色查询优先识别真实房主；迁移f16d80d97501补齐缺失房主成员，并校正房主角色，不修改其他成员。已只读确认本地迁移生效、房间1成员身份恢复。

验证：相关32项测试通过，覆盖详情、成员、消息、WebSocket、非成员拒绝及重复数据修复。

验收：刷新后重新进入房间1，检查聊天、成员列表和同步连接。无新依赖，未启动或重启服务。
