# 第一阶段界面修订报告

新建/编辑改为居中遮罩弹窗，复用 BaseDialog，支持内部滚动、未保存确认和焦点恢复。新增 BaseNumberInput，使用项目图标、圆角和主题色替换原生数字按钮，支持输入、增减、留空及边界限制，中英文提示齐全。

验证：前端8项现有测试、类型检查与构建通过。本轮未改后端，无新增依赖或迁移，未启动服务。

请在 `/admin/videos` 检查新建和编辑弹窗的居中、遮罩、窄屏滚动、Esc/取消、未保存确认，以及年份增减、键盘输入和上下限。浏览器视觉与交互验收待你完成。
