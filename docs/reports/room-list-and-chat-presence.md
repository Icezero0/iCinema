# 房间列表与聊天在线状态修复

聊天记录默认把发送者设为在线，成员信息未到或发送者已退出时会误显绿点。现默认离线，房间聊天仅采用实时成员状态，连接断开时也不沿用在线状态。主页原来等待“我的房间”和公开目录同时成功，现解除依赖，避免公开目录失败阻塞已加入房间。

11项前端测试、2项后端房间列表测试及类型检查通过。请验收离线用户历史消息头像、主页已加入房间。生产环境缺房的具体原因尚未确认，需要核对该环境 `/api/v1/users/me/rooms` 响应。未修改生产数据，无新增依赖，未启动服务。
